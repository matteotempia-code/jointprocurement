import Link from "next/link";
import { DataTable, EmptyRow, PageHeader, StatusChip } from "@/components/ui";
import { requireRoles } from "@/lib/auth";
import { prisma } from "@/lib/prisma";
export default async function TechnicalProducts(){const context=await requireRoles(["PROCUREMENT_MANAGER","PROCUREMENT_ADMIN"]),states=await prisma.productTechnicalState.findMany({where:{organizationId:context.organization.id,status:{not:"COMPLETE"}},include:{canonicalProduct:{include:{category:true}},organization:true},orderBy:[{status:"asc"},{completenessPercent:"asc"}],take:100});return <main className="phase2-page"><PageHeader eyebrow="Product Intelligence" title="Prodotti con evidenze incomplete" description="Le lacune documentali impediscono l’approvazione tecnica per nuovi acquisti, senza alterare lo storico."/><DataTable label="Completezza tecnica"><thead><tr><th>Prodotto</th><th>Categoria</th><th>Completezza</th><th>Mancanti</th><th>Conflitti</th><th>Stato</th></tr></thead><tbody>{states.length?states.map(state=><tr key={state.id}><td><Link href={`/products/${state.canonicalProductId}?tab=technical`}>{state.canonicalProduct.name}</Link></td><td>{state.canonicalProduct.category.name}</td><td>{state.completenessPercent}%</td><td>{state.missingCount}</td><td>{state.conflictCount}</td><td><StatusChip variant={state.status==="CONFLICTED"?"danger":"warn"}>{state.status}</StatusChip></td></tr>):<EmptyRow colSpan={6}>Nessuna lacuna tecnica registrata.</EmptyRow>}</tbody></DataTable></main>}
